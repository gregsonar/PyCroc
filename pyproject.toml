[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "pycroc"
version = "0.2.0"
description = "TUI GUI-клиент для croc на Textual"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "gregsonar" }]
dependencies = [
    "textual>=8.2",
    "qrcode>=8.0",
    "tomlkit>=0.13",
    "platformdirs>=4.2",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.2",
    "pytest-asyncio>=0.24",
    "pytest-timeout>=2.3",
    "ruff>=0.11",
    "mypy>=1.13",
    "types-qrcode>=8.0",
]

[project.scripts]
pycroc = "pycroc.ui.app:main"

[tool.hatch.build.targets.wheel]
packages = ["src/pycroc"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.mypy]
python_version = "3.11"
strict = true
packages = ["pycroc"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
# Страховка от зависаний в CI: на Windows ProactorEventLoop (особенно py3.11)
# отмена/закрытие subprocess-транспорта в asyncio иногда дедлочит. Таймаут
# превращает вис в быстрый фейл, а "thread" (обязателен на Windows, где нет
# SIGALRM) печатает стек всех потоков — видно, где именно застряло.
timeout = 60
timeout_method = "thread"
