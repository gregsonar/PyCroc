[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "pycroc"
version = "0.1.0"
description = "TUI GUI-клиент для croc на Textual"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "gregsonar" }]
dependencies = [
    "textual>=0.60",
    "qrcode>=7.4",
    "tomlkit>=0.13",
    "platformdirs>=4.2",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0",
    "pytest-asyncio>=0.23",
    "ruff>=0.6",
    "mypy>=1.10",
]

[project.scripts]
pycroc = "pycroc.ui.app:main"

[tool.hatch.build.targets.wheel]
packages = ["src/pycroc"]

[tool.ruff]
line-length = 100
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "UP", "B"]

[tool.mypy]
python_version = "3.11"
strict = true
packages = ["pycroc"]

[tool.pytest.ini_options]
asyncio_mode = "auto"
testpaths = ["tests"]
